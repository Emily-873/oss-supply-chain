# Keep the build context minimal: the image only needs the npm manifests from
# scripts/. The book content is bind-mounted at runtime, not copied into the
# image, and pandoc + Libertinus are now installed from the OS / GitHub release.
*
!scripts/package.json
!scripts/package-lock.json
